[package]
name = "apppw-capture"
version = "0.1.0"
edition = "2024"

[dependencies]
windivert = { version = "0.6.0", features = ["vendored"] }
apppw-core = { path = "../apppw-core" }
etherparse = "0.13"
